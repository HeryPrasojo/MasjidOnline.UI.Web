(function ()
{
    window.addEventListener('load', onLoaded);

    function onLoaded()
    {
        const pageLoading = document.getElementById('pageLoading');

        pageLoading.remove();
    }
})();
